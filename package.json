{
  "name": "SolaRate-Ignition",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "npm run clean && tsc --project tsconfig.json && vite build",
    "clean": "rm -rf dist",
    "deploy": "npm run clean && npm run build && gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "typescript": "^4.6.3",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "nvm": "0.0.4"
  }
}
